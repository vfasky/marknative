{
  "name": "notecard",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "typecheck": "bunx tsc --noEmit",
    "test:render": "bun run tests/run-all.ts",
    "test": "bun run typecheck && bun run test:render"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.3",
    "@napi-rs/canvas": "^0.1.97",
    "marked": "^17.0.5",
    "textura": "^0.2.1"
  }
}
